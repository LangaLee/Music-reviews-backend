import seedData from "./seed";
import {
  userData,
  genreData,
  reviewData,
  commentData,
  likesData,
} from "../devData/index";

seedData(userData, genreData, reviewData, commentData, likesData);
